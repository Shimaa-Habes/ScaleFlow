# Install everything with:   pip install -r requirements.txt

pandas>=2.0        # dataframes / CSV handling
numpy>=1.24         # numeric arrays, math
scikit-learn>=1.3   # models, pipelines, metrics, cross-validation
matplotlib>=3.7     # charts (confusion matrix, EDA, feature importance)
joblib>=1.3         # saving/loading the trained model
